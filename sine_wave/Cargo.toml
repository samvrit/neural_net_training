[package]
name = "sine_wave"
version = "0.1.0"
edition = "2021"

[dependencies]
ndarray = { version = "0.16", features = ["serde"] }
rand = "0.8.5"
plotters = "0.3"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"